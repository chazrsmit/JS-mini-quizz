//demander le prénom de l'utilisateur

setTimeout(prenom, 1000) //Timeout pour que le message d'accueil n'apparaisse pas directement

function prenom () {
let prenom = prompt("Bienvenue ! Quel est ton prénom ?")
}